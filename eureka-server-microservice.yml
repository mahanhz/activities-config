config:
  message: Greetings from the EUREKA server microservice [using the BASE config]

server:
  port: 13303

management:
  port: 13304
  context-path: /eureka/manage

eureka:
  dashboard:
    path: eurekaWebUI
  instance:
    statusPageUrlPath: ${management.context-path}/info
    healthCheckUrlPath: ${management.context-path}/health

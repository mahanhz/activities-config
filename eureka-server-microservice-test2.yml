config:
  message: Greetings from the EUREKA server microservice [using the TEST2 config]

eureka:
  instance:
    hostname: 192.168.1.34
  client:
    serviceUrl:
      defaultZone: http://${peer1.hostname}:${server.port}${server.context-path}/
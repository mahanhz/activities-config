config:
  message: Greetings from the EUREKA server microservice [using the BASE config]

server:
  port: 13303
  context-path: /eureka

management:
  port: 13304
  context-path: ${server.context-path}/manage

error:
  whitelabel:
    enabled: false

eureka:
  # defaultZone.context-path is a made up eureka property for reuse
  defaultZone:
    context-path: eurekaZone
  dashboard:
    path: dashboard
  instance:
    preferIpAddress: true
    statusPageUrlPath: ${management.context-path}/info
    healthCheckUrlPath: ${management.context-path}/health
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/${eureka.defaultZone.context-path}/

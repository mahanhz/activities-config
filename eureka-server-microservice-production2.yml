config:
  message: Greetings from the EUREKA server microservice [using the PRODUCTION2 config]

eureka:
  instance:
    hostname: 192.168.1.34
  client:
    serviceUrl:
      defaultZone: http://192.168.1.33:${server.port}${server.context-path}/

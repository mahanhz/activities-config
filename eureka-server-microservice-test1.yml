config:
  message: Greetings from the EUREKA server microservice [using the TEST1 config]

eureka:
  instance:
    hostname: 192.168.1.33
  client:
    serviceUrl:
      defaultZone: http://${peer2.hostname}:${server.port}${server.context-path}/
